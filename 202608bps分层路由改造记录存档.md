# 2026-08-08 MP 跨仓库指挥层接入 + 分层路由改造记录

背景：搭建「一人公司」Agent 编排平台——用户只对总管 Agent **MP（管理合伙人）** 下命令，不关心底层哪个仓库/skill 执行。经两轮调研（多 agent 编排框架市场 + Claude Code 官方机制）定案：**不引入外部编排框架**（LangGraph/CrewAI/Dify/n8n 均否决——资产是 prompt 非代码，迁移是净负债），基于 Claude Code 原生能力做 **hub-and-spoke 分层路由**；跨仓库总调度放新建独立仓库 **`ztl-mp`**（MP=领导，各业务仓库平等如员工），执行引擎沿用 **ztl-symphony**（按工作区起会话）。

改造依据（路由过载证据）：工具/skill 数从 4 扩到 51 时模型选择准确率从 43% 崩到 2%（Berkeley Function Calling Leaderboard）；Anthropic 官方以「15 skill + 10 MCP 工具」为上下文过载反例，而本仓库改造前 41 skill + 8 agent = 49 条 description 常驻竞争。

## 一、本仓库改动：14 个 skill 加 `disable-model-invocation: true`

连同既有 3 个（accrual-cash-voucher-gen / accrual-cash-review-reconcile / lao-annual-base-build，2026-07 已隐藏）共 **17 个隐藏**；常驻路由条目 **49 → 32**（24 可见 skill + 8 agent）。

选取标准：**仅（或主要）经编排 agent dispatch 的管道内部步骤，或低频逃生口**；用户直呼型、消歧充分的 skill 保持可见。三条不变量：①`/name` 显式调用不受影响；②编排 agent 按绝对路径读 SKILL.md dispatch 不受影响（铁律 1 机制，与 Skill 工具无关）；③被隐藏触发词由上游编排 agent description 承接。

### 隐藏清单与触发词承接矩阵（本次 14 个）

| Skill | 定位 | 触发词承接方 |
|---|---|---|
| fx-revaluation-lao | 期末调汇（其 description 原本就警告"默认路由勿直接命中"） | accounting-loop-flow 卡片2D / lao-month-end-close 事项⑤ |
| fx-journal | 交易级换汇 584 清算（月结事项⑤A 被调方） | lao-month-end-close（"月底分录/调汇"） |
| sez-surcharge-filing-lao | 特区附加（税务 flow 串行步骤） | lao-tax-monthly-filing-flow（"特区附加"） |
| ztl-tax-summary | 税金汇总（税务 flow 收尾步骤） | lao-tax-monthly-filing-flow（"税金汇总"） |
| vat-input-recon | 进项核对（并行组B / 卡片2A） | lao-tax-monthly-filing-flow / accounting-loop-flow 卡片2A |
| vat-output-recon | 销项核对（并行组B / 卡片2A） | 同上 |
| wht-foreign-payment-ledger | 境外付款 WHT 台账（路径A） | wht-filing-flow（description 已全量覆盖其触发词） |
| vat-input-selfdeclare | 境内无税票补税台账（路径B） | wht-filing-flow（同上） |
| tb-balance-verify | TB 独立重算验证（质控硬卡点） | qc-review-accounting-flow |
| pl-simulation-review-lao | 模拟利润表三项分析（质控第五步） | qc-review-accounting-flow |
| lao-pbc-checklist-gen | 资料收集清单（卡片0 助手） | accounting-loop-flow 卡片0（"资料齐套"） |
| lao-period-recon-check | 域内自查（排障逃生口） | qc-review-accounting-flow 消歧句提及；主要靠 /name |
| lao-new-coa-upload-gen | 新增科目上传模板（低频逃生口） | **仅 /name 直呼**（无上游 description 承接） |
| lao-legacy-client-migrate | 老客户迁 .RULE（一次性低频） | **仅 /name 直呼**（同上） |

> ⚠️ 最后 2-4 项为**弱承接**：用户如用自然语言说"整理新增科目上传模板"/"迁移到RULE"将不再自动命中，需 `/lao-new-coa-upload-gen` 式直呼或经 loop agent 转发。属有意取舍（低频+词面独特，误路由代价小于常驻成本）；若实际使用中不便，把对应触发句加进 accounting-loop-flow description 即可回补。

### 保持可见的 24 个 skill（用户直呼型）

会计+质控 11：lao-journal-extract、lao-legacy-tb-carryover、lao-period-folder-setup、lao-pbc-file-organizer、lao-upload-exporter、lao-fs-notes、bank-balance-recon-lao、ar-ap-multiccy-lao、fa-depreciation-lao、voucher-review-ui、mgmt-new-bps-client；税务 5：vat-input-scanner、vat-output-scanner、pit-payroll-lao、pit-sez-lao、wht-contract-risk-scan；通用工具 8：tool-excel-recalc、tool-table-diff、tool-data-cleaning、tool-pdf-scan、tool-bol-exchange-rate、file-batch-rename、file-dedupe、doc-template-fill。

## 二、claude.ai 账户级陈旧副本停用清单（需用户在 claude.ai → Settings → Skills / Cowork 插件管理里手工处理，仓库侧无法代办）

这些账户级副本与仓库内 skill 同场竞争路由，是"skill 时好时坏"的直接原因：

| 账户级 skill | 冲突对象 | 建议 |
|---|---|---|
| `fs-notes-lao` | 本仓库 `lao-fs-notes`（2026-07-13 已改名转正，description 几乎逐字相同） | **停用账户级** |
| `bol-exchange-rate` | 本仓库 `tool-bol-exchange-rate`（同上） | **停用账户级** |
| `ztl-content-writing-loop` 与 `ztl-linear-content-writing-loop` | 两者 description **完全相同**（账户级互撞） | **二留一**（留常用者，另一个停用） |
| `wht-processor` | 与 wht-filing-flow 双路径 + wht-foreign-payment-ledger 职责重叠（Claude Project 时代旧流程） | 确认后停用或改写消歧 |
| `journal-cleanup` | 与 lao-journal-extract（入仓）职责相邻 | 确认后停用或改写消歧 |
| `cash-basis-from-journal` / `fs-review-lao` | 本仓库编排引用的库外依赖（CLAUDE.md「库外依赖」条），**保留**，但注意其 description 与 accrual 系的消歧 | 保留 |

## 三、跨仓库组织架构定案（详见 ztl-mp 仓库 workspace-registry.md，本节只登记）

- **MP 领导**：`ztl-mp`（新建）——章程/工作区注册表/全局路由表/躯干蓝图。
- **员工**：`ztl-bps-workspace`（=D:\ztl-agent-v2，会计+税务）、`ztl-agent-mgmt`（管理+报价）、`laos-compliance-kb`（=D:\laos-wiki，Lao-wiki；摄入流 laos-kb-ingest）、`ztl-content`（公众号/文章）、`lao-law-lib`（法规翻译 OCR）。
- **执行引擎**：`ztl-symphony`（按工作区起会话）；**基础设施**：`ticktick-mcp`。
- **确认弃用**（2026-08-08，MP 永不派单）：ztl-agent、ZTL-SYSTEM、ztl-ai-system、Monthly-Tax-Submit、Mr.-ZTL、sole-prop-bookkeeping；**不纳入**：ztl-laobooks（试验品）、my-web-blog。
- 躯干（第二期）：Telegram Bot 网关 → symphony 起 MP 会话 → 按区派发；同步盘双端同步代账数据；滴答清单任务看板；Routines 定时。

## 四、同步情况

- MANIFEST.md：最后更新行 + 触发机制节增「分层路由」段 + 各域 Skill 表 17 行 ⛔ 标注 + 工作区结构表增 GitHub 仓库对照段。
- CLAUDE.md：触发方式段增第④条 + MP 跨仓库调度说明。
- ARCHITECTURE.md：最后更新行登记（跨仓库路由权威在 ztl-mp，本仓库只登记）。
- 各 SKILL.md：仅 frontmatter 插入一行 `disable-model-invocation: true`，description 正文零改动（**有意保守**：触发词体系是数月打磨产物，隐藏后其 description 不再进上下文、无常驻成本，故不重写）。
